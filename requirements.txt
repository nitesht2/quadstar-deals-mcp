python-dotenv>=1.0.0
requests>=2.31.0
requests-oauthlib>=1.3.0
playwright>=1.40.0
scrapling[all]>=0.4.0
pydantic>=2.7.4
openai>=1.50.0  # DeepSeek + OpenRouter (both OpenAI-compatible)
# API server + scheduler
fastapi>=0.115.0
uvicorn>=0.32.0
apscheduler>=3.10.0
# Discord bot: deal cards + approval buttons
discord.py>=2.3.0
# Profiles loader (profiles/<name>.yaml)
PyYAML>=6.0
# Postiz image upload — JWT signing for internal media API
PyJWT>=2.8.0
# MCP server — typed tool seam Hermes drives the pipeline through (src/mcp_server.py)
mcp>=1.27.0
